import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function AiToolsPage() {
  return (
    <PlaceholderPage
      title="AI Tools"
      description="Qualification engine, outreach studio, verification helpers — one toolkit for every lead."
      features={[
        "AI lead scoring already runs inside Lead Finder",
        "Outreach Studio ships on each lead detail page",
        "Ask Expert drafts scripts, offers, and funnel fixes",
        "Upcoming: batch AI rewrite + verification dashboard",
      ]}
    />
  );
}
